<!doctype html><html><head><meta charset="utf-8"><style>body{font-family:DejaVu Sans,Arial;font-size:10px;color:#17202a}h1{font-size:18px;margin:0 0 4px}.meta{margin:0 0 14px;color:#566573}table{width:100%;border-collapse:collapse}th,td{border:1px solid #aeb6bf;padding:5px;vertical-align:top}th{background:#eaecee;text-align:left}.filters{margin:8px 0}</style></head><body>
<h1>PT Ali Properti Indonesia</h1><h2>{{ $title }}</h2><div class="meta">Dicetak {{ $printedAt->format('d/m/Y H:i') }} oleh {{ $printedBy }}</div>
<div class="filters"><b>Filter:</b> {{ collect($filters)->filter(fn($v) => $v !== null && $v !== '')->map(fn($v,$k) => str_replace('_',' ',$k).': '.$v)->join(' | ') ?: 'Tanpa filter tambahan' }}</div>
<table><thead><tr>@foreach($columns as $column)<th>{{ $column }}</th>@endforeach</tr></thead><tbody>@forelse($rows as $row)<tr>@foreach($row as $cell)<td>{{ $cell }}</td>@endforeach</tr>@empty<tr><td colspan="{{ count($columns) }}">Tidak ada data.</td></tr>@endforelse</tbody></table></body></html>
