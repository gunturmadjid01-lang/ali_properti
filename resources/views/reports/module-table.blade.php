<!doctype html><html><head><meta charset="utf-8"><title>{{ $title }}</title><style>body{font-family:Arial,sans-serif;font-size:11px;color:#172033}h1{font-size:20px}table{width:100%;border-collapse:collapse;margin-top:16px}th,td{border:1px solid #cbd5e1;padding:7px;text-align:left}th{background:#e2e8f0}.meta{color:#64748b}</style></head><body><h1>{{ $title }}</h1><div class="meta">Dicetak {{ $printedAt }}</div><table><thead><tr>@foreach(collect($rows)->first() ? array_keys((array) collect($rows)->first()) : [] as $column)<th>{{ str($column)->replace('_',' ')->title() }}</th>@endforeach</tr></thead><tbody>@forelse($rows as $row)<tr>@foreach((array)$row as $value)<td>{{ is_scalar($value) ? $value : json_encode($value) }}</td>@endforeach</tr>@empty<tr><td>Belum ada data.</td></tr>@endforelse</tbody></table><script>if(!location.pathname.endsWith('.xls'))window.print()</script></body></html>
