import { Head, router } from '@inertiajs/react';
import { ArrowLeft, Edit3 } from 'lucide-react';
import { Button } from '../../../../Components/UI';
import AdminLayout from '../../../../Layouts/AdminLayout';
export default function Show({title,baseUrl,row}){const fields=[['Pelanggan',row.customer],['Kode Pelanggan',row.kode_costumer],['Waktu Aktual',row.tanggal_follow_up],['Metode',row.metode_follow_up],['Hasil',row.result_label],['Tingkat Minat',row.interest_label],['Keseriusan',row.status_serius],['Kemajuan',row.progress_kemampuan],['Status',row.status_label],['Rencana Berikutnya',row.rencana_follow_up_at||'-'],['Input Oleh',row.input_oleh],['Kunci',row.record_status_label],['Catatan',row.catatan||'-'],['Kendala',row.obstacle||'-'],['Tindakan Berikutnya',row.next_action||'-']];return <><Head title={title}/><div className="grid gap-6"><section className="rounded-lg border bg-white/80 p-6 shadow-soft dark:border-white/10 dark:bg-white/8"><p className="text-xs font-extrabold uppercase tracking-widest text-ink-soft">Marketing / Tindak Lanjut / Detail</p><h1 className="mt-2 text-3xl font-extrabold">{title}</h1><div className="mt-5 flex gap-2"><Button variant="outline" onClick={()=>router.visit(baseUrl)}><ArrowLeft size={16}/> Kembali</Button>{row.can_edit&&<Button onClick={()=>router.visit(`${baseUrl}/${row.id}/edit`)}><Edit3 size={16}/> Ubah</Button>}{row.attachment_url&&<Button as="a" variant="outline" target="_blank" href={row.attachment_url}>Lihat Bukti</Button>}</div></section><section className="grid gap-4 rounded-lg border bg-white/80 p-6 shadow-soft md:grid-cols-2 dark:border-white/10 dark:bg-white/8">{fields.map(([label,value])=><div className={['Catatan','Kendala','Tindakan Berikutnya'].includes(label)?'md:col-span-2':''} key={label}><p className="text-xs font-extrabold uppercase tracking-wider text-ink-soft">{label}</p><p className="mt-1 whitespace-pre-wrap font-semibold">{value}</p></div>)}</section></div></>};
Show.layout=page=><AdminLayout title={page?.props?.title??'Detail Tindak Lanjut'}>{page}</AdminLayout>;
